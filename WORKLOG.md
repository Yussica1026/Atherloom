# Atherloom 开发工作日志

## 2026-08-02 · Android v0.5.48

- 撤销“拦截 PDF、要求转 TXT”的临时方案，改为 Android 原生后台线程通过 PDFBox 读取 `content://` 输入流并提取正文，再交给 WebView 共读界面。
- 初始化 `PDFBoxResourceLoader`；PDF 限制为 24 MB、400 页和 600,000 字。文字型 PDF 可正常共读，扫描图片型 PDF 给出明确错误且不闪退。
- 对话切换器改为两步删除：长按约 0.7 秒只显示当前行的红色“删除”按钮，再点击按钮才执行持久化删除；不再长按即删。
- Android 87 项测试与网页真实交互回归纳入三轮验证；最终三轮连续全部通过。
- 发布提交 `e4ead8d`，versionCode `54`，版本 `0.5.48-standalone`，标签 `v0.5.48`。
- v0.5.48 实机发现 WebView 在 0.7 秒前触发 `pointercancel`，删除按钮未显露；修正版把触发提前到 0.48 秒、拦截系统上下文菜单，并加入移动阈值和 Android 式取消事件回归，版本升级为 `v0.5.49`（versionCode 55）。
- APK：<https://github.com/Yussica1026/Atherloom/releases/download/v0.5.48/Atherloom-Android.apk>

## 2026-07-29 · v0.5.36

### 用户反馈与处理

- 修复删除对话后必须刷新页面，左侧列表才更新的问题。
- 修复删除当前对话后，左侧旧标题、左上角对话切换器及聊天正文仍然残留的问题。
- 左上角对话切换器为每条对话增加独立删除入口。
- 删除完成后重新读取持久化数据，清空被删除对话的消息和版本缓存，并安全切换到下一段对话；没有剩余对话时进入真正的新对话。
- 新增真实对话删除回归测试，同时检查数据库、左侧列表、标题切换器、当前状态和正文。
- 共读提问留在共读界面，使用共读专属记录，不跳回主聊天。
- 修复本地书籍编码识别，兼容 UTF-8 与常见中文旧编码，减少乱码。
- 人格指令中的变量入口改为聊天内容模板；变量可点击插入模板。
- 聊天内容模板支持 `{{role}}`、`{{message}}`、`{{time}}`、`{{date}}` 并提供实时预览。
- 用户称呼使用用户名或用户指定称呼，不再使用“宿主”等内部术语。
- 新增独立图片理解线路，可把 DeepSeek、GLM 等文本线路的图片请求交给专用视觉模型。
- 新增独立网页搜索线路：内置免费搜索、Tavily、Brave Search 和自定义兼容接口。
- 内置搜索使用文章索引与中文百科兜底，并处理“随便看看”等宽泛请求。
- 网页与 Android 共用搜索线路设置；Android 原生桥支持 Tavily、Brave 与自定义搜索接口。
- 欲望状态改为每回合更新并注入模型上下文。
- 新增可关闭的“正在输入”状态；只共享输入时长、停顿和清空状态，不读取未发送文字。
- 用户头像和人格头像的背景、边框与文字颜色改为跟随当前主题。

### 发布与验证

- 网页资源版本：`0507`。
- Service Worker：`v106`，缓存壳版本 `v119`。
- Android：`0.5.36-standalone`，versionCode `42`。
- 发布提交：`013458e`。
- Git 标签：`v0.5.36`。
- 后端自动测试：73 项全部通过。
- 移动端网页交互测试全部通过，覆盖删除、搜索、共读、模板、欲望注入、主题头像和 Android 桥。
- GitHub Pages 部署成功。
- Android 固定签名 APK 构建与 GitHub Release 发布成功。

### 本机工具备注

- 当前电脑尚未安装 GitHub CLI（命令名 `gh`）。
- Windows 安装命令：`winget install --id GitHub.cli`。
- 安装后重新打开终端并运行 `gh auth login` 登录。
- 本次发布状态通过 GitHub 公开接口完成核验，未影响发布。
