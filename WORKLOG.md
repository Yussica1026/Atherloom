# Atherloom 开发工作日志

## 2026-08-13 · v0.5.79 Android 普通聊天流式回归修复

- 对照 v0.5.55“Android 原生流式思维开关”记录与 v0.5.78 真机录屏确认：原生 `chatStream` 会逐行读取 SSE 并即时转发，WebView 计时和键盘也持续响应，并非页面或原生读取器冻结。
- 真正回归点位于 Android Standalone 工具循环：由于每轮总有内置工具，普通“亲亲”等闲聊也会先执行一次完整的非流式工具探测；模型整段生成结束后才一次性送进前端，绕过了已存在的原生流式分支。
- 改为按用户本轮意图决定是否进入工具探测。普通闲聊直接使用 `nativeStreamResponse`；明确涉及记忆、联网、生活簿、日记、游戏、乌有乡或自主活动时才保留工具循环。
- 流式语义澄清：普通聊天是模型正文到达一段就显示一段；工具请求则分为“模型决定工具（不展示正文）→执行工具→生成最终回复”。工具决策阶段不属于给用户看的正文，因此不做逐字显示；当前工具执行后的最终回复仍可能整段显示，后续可单独改为流式，不能再把它与普通聊天流式混为一谈。
- Android 版本升级为 `0.5.79-standalone`（versionCode 83）；118 项自动化测试、JavaScript 语法和差异检查通过。

## 2026-08-13 · v0.5.78 AI 记忆重要度分级与旧记忆复评

- AI 新增长期记忆时必须自行填写 0.1～1.0 重要度，不再静默使用统一的 0.5；工具说明和系统规程给出核心、重要、常用、一般、低五档判定标准，并明确禁止把所有记忆都设为 1。
- 召回仍以相关性为前提；相关候选之间使用重要度乘数排序。记忆管理列表改为星标、重要度、更新时间依次排序，编辑滑杆与详情显示“核心/重要/常用/一般/低”及一位小数。
- 新增“AI 评估重要度”：选择任意可用线路，默认只处理仍为 0.5 的旧记忆；每批最多 80 条，可连续生成下一批。AI 返回逐条分数与简短理由；结果仅进入预览，用户可以逐条取消勾选，确认后才写入。
- 桌面后端应用复评时写入 `regrade` 审计记录，保留修改前重要度、修改后重要度与 AI 理由；Android Standalone 同步支持预览、勾选和本地应用。
- 六款小游戏统一把自主游玩的完整结果写入各自游戏房间；从聊天发起游戏时，完整过程留在游戏内，聊天只要求一至两句最终汇报。游戏库自主活动产生的内部提示和完整回复不再占据聊天气泡，但仍保留为后续上下文。
- 模型首 token 等待阶段每秒显示累计用时，并明确提示可以继续编辑下一条，收到正文后自动停止计时。
- Android Standalone 已通过模拟 OpenAI 兼容线路的端到端测试，覆盖旧记忆加载、AI JSON 分级、预览、勾选、应用和本地持久化；390×844 手机视口通过。
- 117 项自动化测试、JavaScript 语法、Git 差异检查和移动端端到端测试通过；Android 版本升级为 `0.5.78-standalone`（versionCode 82）。

### 工作日志状态校正

- 历史条目中的“本地，未发布”描述的是当时状态，不再作为当前待办判断依据；2026-08-09 的记忆操作说明、完整管理和脑式记忆收尾均已被后续 v0.5.56～v0.5.77 系列吸收。
- 2026-08-02 的七项稳定性修复也已进入后续 Android 版本；保留原条目作为过程记录，不再重复列为发布阻塞项。
- 仍然真实未闭环的外部事项只有：Mate 30 Pro 特定机型真机覆盖，以及 OnlyRouter 区域 403；前者需要对应设备，后者需要服务商允许的出口或更换可用模型，不能由前端代码绕过。

## 2026-08-13 · v0.5.77 记忆召回关键词覆盖与近义去重

- 网页后端与 Android Standalone 统一改为关键词覆盖优先：标题关键词获得更高权重，英文词、模型名、数字及单字查询也可参与精确匹配。
- 召回选择增加近义硬去重；两条记忆正文高度相似时只注入得分更高的一条，不再用换一种说法的内容凑满结果数量。
- 已覆盖关键词不再重复加分，尚未覆盖的新关键词获得额外权重，让同一轮结果尽量包含用户问题里的不同事项。
- 星标只提升真正相关的记忆，不再无条件注入；Android 取消无命中时塞入最近三条记忆的回退行为。
- 修正召回反馈循环：读取记忆只更新召回时间并轻量增强强度，不再刷新“用户确认时间”，避免同几条旧记忆长期霸榜。
- 版本升级为 `0.5.77-standalone`（versionCode 81）；114 项自动化测试与 JavaScript 语法检查通过。

## 2026-08-13 · v0.5.76 录屏卡顿定位与乌有乡口语识别

- 逐秒检查 19 秒 Android 真机录屏：点击发送后不足 1 秒即出现“正在生成”、人格输入状态和停止按钮，页面没有失去响应；主要等待发生在模型返回首 token 之前，录屏中该会话热上下文已经达到 46,739 tokens。
- 生成状态先于自动压缩和网络请求完成两帧绘制，确保前置工作不会吞掉点击反馈；回复结束后立即解锁输入区，记忆刷新移出关键交互路径。
- 聊天口语识别补充“你去玩乌有乡”“你去乌有乡旅行”“去乌有乡自己走走”等说法，可读取指定步数并要求模型真实调用 `atherloom_nowhere`，完成后再回来简短汇报。
- 自动 Token 阈值压缩继续作为大上下文首 token 延迟的主要治理手段；版本升级为 `0.5.76-standalone`（versionCode 80）。

## 2026-08-13 · v0.5.75 游戏对话、Token 上限与自动压缩

- 根据 Android 真机反馈统一游戏内表达规则：游戏库发起的过程与回复以游戏房间、心里话和行动记录为主；六款内置小游戏共用可滚动房间对话，乌有乡心里话改为竖向滚动，完整换行显示，不再用省略号截断行动详情。
- 定位乌有乡单轮消耗异常：工具此前在每一步返回完整旅程状态，包含持续增长的路径、标记、明信片与纪念品；多轮工具调用又累计携带旧结果，导致输入 Token 随旅程历史快速膨胀。
- 乌有乡模型工具返回改为固定大小当前快照：只含当前位置、时间、距离、天气、本次产物与各类数量；完整路径继续仅保存在本机并供地图绘制，不再注入模型。
- 不对其他工具设置统一字符截断，避免文件读取、代码审查、日志排障和 MCP 返回丢失关键内容；本次只针对已确认膨胀的乌有乡返回结构做瘦身。
- 乌有乡地图增加数据签名：轮询结果未变化时跳过 SVG 数据层重绘，减少 Android WebView 短暂无响应和地图闪空。
- 流式回复结束时立即刷完前端剩余字符队列并释放输入框，不再等待逐字动画额外耗时。
- 自动总结页新增 Token 阈值压缩：用户可开关、填写 1,000～1,000,000 Token 阈值并指定独立压缩线路；达到阈值后在回复前压缩旧对话，保留最近一轮，失败时继续聊天。
- 版本升级为 `0.5.75-standalone`（versionCode 79）；113 项自动化测试与 JavaScript 语法检查通过。

## 2026-08-13 · v0.5.74 乌有乡手机折叠面板

- 根据 v0.5.73 Android 真机截图确认世界地图已经恢复，但原版固定定位的当前位置卡片在手机短视口中覆盖地图，和明信片入口形成拥挤叠层。
- 乌有乡当前位置卡片改为可点击折叠：手机宽度下默认收起为地点标题条，点击展开完整地点说明与统计，再次点击收起；桌面宽度仍默认展开。
- 手机布局同步隐藏占空间的地图图例，并收紧品牌和当前位置面板边距，保留完整地图视野及明信片墙入口。
- 新增 390×640 手机视口浏览器交互测试，验证默认收起、展开、再次收起与无脚本异常；112 项自动化测试通过。
- 版本升级为 `0.5.74-standalone`（versionCode 78）。

## 2026-08-13 · v0.5.73 多线路压缩、自动记忆召回与乌有乡地图修复

- 主动压缩弹窗新增独立线路选择：默认沿用当前聊天线路，遇到上游 HTTP 502 时可换用其他已启用线路重试，且不会改变当前对话绑定线路。
- 明确并加强每轮回复前的记忆自动召回：根据用户刚发送的话执行向量与关键词混合检索，将相关记忆作为内部上下文交给 AI 后再生成回复；向量线路失败时继续回退关键词召回。
- 记忆注入增加使用约束：相关时自然利用、不相关时忽略，不向用户复述内部标签或记忆 ID，也不对已经注入的内容重复要求搜索。
- 根据 Android 真机截图定位乌有乡地图空白：`srcdoc` 的资源基址少了一层 APK `assets/` 目录，导致 `static/world110m.js` 世界地图数据未加载，只能显示羊皮纸底色。
- Android 内嵌乌有乡基址修正为 `appassets.androidplatform.net/assets/assets/nowhere/`，并同步修正 Standalone 脚本地址；网页版加载路径保持不变。
- 版本升级为 `0.5.73-standalone`（versionCode 77）；112 项自动化测试、JavaScript 语法检查与差异格式检查通过。

## 2026-08-11 · AstrBot 乌有乡工具结果上下文注入修复

- 根据实际对话日志定位：AstrBot 插件使用 `yield event.plain_result(...)` 时，结果会走“直接发给用户”流程，Agent 下一轮收到的却是“工具没有返回值”，因此 AI 看不到自己刚刚旅行的地点和环境。
- 修复 `astrbot_plugin_nowhere`：`nowhere_travel` 改为返回结构化纯文本 `NOWHERE_TRAVEL_RESULT`，由 AstrBot 转换为真正的 tool result，下一轮上下文可读取地点、天气、声音、地形和观察内容。
- 工具异常也改为返回上下文可见的错误结果，避免 AI 误判为没有调用成功或没有收到结果。
- 增加“冰岛 雷克雅未克”“雷克雅未克”等中文地点别名，统一归一化到 `Reykjavik`。
- 插件版本更新至 `1.0.1`，补充 5 项插件契约测试，全部通过。
- AstrBot 提交：`4137543 chore: release context injection fix`。

## 2026-08-11 · v0.5.72 乌有乡入口与 AI 行动直播

- 根据 Android 真机截图定位到 AssetManager 路径多一层 `assets/`：同步任务把 `frontend/assets/nowhere/index.html` 打包为 `assets/nowhere/index.html`，而旧版错误读取了 `nowhere/index.html`。
- 修正原生资源桥与前端调用路径，并保留固定路径白名单，避免资源读取接口被用于任意 APK 文件。
- 游戏观察页新增“AI 心里话”、最新行动说明和最近 12 步行动轨迹。显示内容来自真实 `atherloom_nowhere` 工具调用及其结果，不暴露或伪造模型隐藏推理。
- Python 后端与 Android Standalone 工具循环均逐步发送 `nowhere` 事件，开始、停止和错误状态也会即时反馈。
- 版本升级为 `0.5.72-standalone`（versionCode 76）；116 项自动化测试、JS 语法检查和完整移动端浏览器冒烟通过。

## 2026-08-11 · v0.5.71 主动压缩与乌有乡 Android 加载修复

- 聊天顶栏新增主动压缩按钮和轮数选择窗口。压缩调用当前会话线路，写入会话摘要与摘要版本记录；被压缩消息继续显示，但从后续模型热上下文排除，最近一轮原文强制保留。
- Android Standalone 使用独立的压缩消息 ID 集合，并把会话摘要作为 `<conversation_summary>` 注入后续请求，与桌面后端保持相同语义。
- 用户真机截图确认乌有乡 iframe 报 `net::ERR_INVALID_RESPONSE`。修复后不再让 WebView 直接导航嵌套资产 URL，而由 `MainActivity.readBundledAsset` 读取入口，前端通过带固定 base URL 的 `srcdoc` 装载。
- 补齐原版观察页需要的 `/state`、`/history`、`/marks`、`/sightings`、`/postcards` Android 本地响应映射。
- 版本升级为 `0.5.71-standalone`（versionCode 75）；115 项自动化测试、JS 语法检查和完整移动端浏览器冒烟通过。

## 2026-08-11 · v0.5.70 AI 自主生活与 Android 后台唤醒

### 用户目标

- 给 AI 增加可由用户配置的定时提醒／定时唤醒能力。
- 唤醒后不要求用户继续指挥，由当前人格自行决定去乌有乡旅行、玩内置游戏或写日记。
- Android 退到后台后仍尽量继续执行，并接受前台常驻通知作为运行条件。
- 将此前尚未接通的“让 AI 自己旅行”按钮一并完成。

### 实现内容

- 在“设置 → 插件中心”新增“AI 自主生活计划”：
  - 可启用或关闭计划。
  - 唤醒间隔支持 15 分钟、30 分钟、1 小时、3 小时和6小时。
  - 每日运行上限支持 1～24 次。
  - 乌有乡旅行、内置游戏、活动日记可分别授权。
  - 本机保存最近 30 条自主活动记录。
- 新增统一的前端自主执行器 `AtherloomRunAutonomyWake`：
  - 到点后检查开关、每日上限、当前线路和生成占用状态。
  - 将自主活动要求交给当前人格自行决策。
  - 使用真实的 `atherloom_nowhere`、`atherloom_game_play`、`atherloom_journal_create` 工具，不用文字伪装执行结果。
  - 自主日记运行获得本轮明确授权，不弹出无法在后台操作的确认框。
- 乌有乡旁观页新增一键自主旅行：支持 6、10、12 步，运行中可立即停止，已经产生的旅行存档继续保留。
- Python 后端与 Android Standalone 的工具循环上限由 4 轮提高至 12 轮；整次最多 12 次、每轮最多 4 次的硬限制保持不变。
- Android 新增 `AutonomyService` 前台服务：
  - 用户保存启用配置后启动常驻通知。
  - 按用户设置的间隔唤醒 WebView 中的自主执行器。
  - 通知栏提供“停止计划”操作。
  - 新增通知、前台服务和 dataSync 前台服务权限。
  - 应用进程或 WebView 被系统彻底回收时，通知会提示重新打开 Atherloom；强制停止应用后系统不会继续唤醒。

### 验证与修正

- Python 自动化测试：114 项全部通过。
- JavaScript：`app.js` 与 `standalone.js` 语法检查通过。
- 完整移动端浏览器冒烟测试通过，包括 token 显示、权限、记忆、游戏、阅读、共影、共听和定时留言等既有功能。
- 修正冒烟用例中模型线路受当前会话绑定影响的问题，使 token 数测试使用独立测试线路且测试后完整恢复现场。
- GitHub Actions Android 构建、固定证书签名校验、Artifact 上传与 Release 发布全部通过。

### 发布记录

- 提交：`67a90d1 feat: add Android AI autonomy scheduler`
- 标签：`v0.5.70`
- Android：`0.5.70-standalone`，versionCode 74。
- Actions：`31458296658`，成功。
- APK：`https://github.com/Yussica1026/Atherloom/releases/download/v0.5.70/Atherloom-Android.apk`
- APK 大小：24,773,128 字节。
- SHA256：`70a086881773eebac8db27e889947a13ee683634ddd788f8a27c4754392eecba`。

### 发布边界

- 前台服务能显著提高退到后台后的存活率，但 Android 厂商省电策略仍可能延迟任务。
- 用户应允许通知并将 Atherloom 加入后台运行／电池优化白名单；不要强制停止应用。
- 本次没有触碰用户未跟踪的 `data/`、像素区域图片和 `tests/fixtures/`。

## 2026-08-09 · 思维链 / Android / 云芽事故复盘（必须遵守）

### 前因后果

- 用户反馈前端思维链消失，目标是恢复截图中的“深度思考”展开卡片；同时明确云芽庭院是独立游戏，不得加入 Android APK。
- 早期只验证了请求参数、解析器和前端渲染，没有验证真实 API 返回，也没有先核对 APK 内实际资源。
- 后续连续发布多个版本，反复修改 Android 原生桥、协议判断、思维链解析和云芽入口，造成用户多次安装仍无思维链。

### 已确认的真实问题

- `app.css` 与 `inline.html` 曾存在全局 `.thinking{display:none!important}`，导致思维链即使解析成功也完全不可见。
- Android 原生主聊天和网页独立版部分路径曾漏传 `thinking_enabled:true`；通用 `openai` 协议下的 `deepseek-v4-pro` 也曾未按模型名发送 `thinking`。
- 模型可能把思考放在正文 `<think>...</think>`，旧解析器只识别 `reasoning_content/reasoning`。
- 为隐藏云芽曾错误删除初始化 DOM，导致 `app.js` 启动异常，表现为人格、窗口、API 全部打不开。
- `v0.5.61` 标签实际仍包含隐藏思维链 CSS，未核对标签内容就把它当回退包，是错误发布。

### 不能再犯的错

1. 不能把单元测试通过说成 Mate 30 Pro 真机验证通过；必须区分静态测试、APK 内资源检查、真机 WebView 控制台和真实 API 返回。
2. 不能在未核对 tag commit、APK 内部资源和下载 HTTP 200 前发送版本链接。
3. 不能连续发布猜测性 APK；必须先抓真实请求/响应，再做完整修复。
4. 不能为隐藏独立游戏删除任何 DOM 或初始化节点；只能用通用 Android 标记、CSS 隐藏，并禁用 Android API 入口。
5. 云芽庭院必须与 Android APK 隔离，网页版独立保留。
6. 每次发布前必须完成两轮：102 项回归测试、JS/Python 语法检查、APK 内资源检查、入口/API smoke；任一项未完成不得上传。

### 当前状态

- v0.5.67 已上传；思维链隐藏 CSS 已移除，云芽 Android 入口、列表和接口已禁用，初始化节点保留。
- 尚未完成 Mate 30 Pro 真机验证；后续不得再声称“实机确认”。


## 2026-08-09 · OnlyRouter / OpenRouter Worker 排查

- 读取桌面 `0809.docx`，新增 OnlyRouter Anthropic 原生路由；上游保留 `https://console.onlyrouter.cc`，只注入稳定提示词的 `cache_control: {type: "ephemeral"}` 分层缓存。
- OpenRouter 转发改为 `https://openrouter.ai/api`，兼容 `/openrouter/v1/...` 与 `/openrouter/api/v1/...`，修复缺少或重复 `/api` 导致的 404；并移除强制 Amazon Bedrock / 禁止 fallback 限制。
- 删除 Worker 稿中的 Kiro 认证、Kiro Runtime、Google Antigravity/Sandbox 路由；A 社默认、OpenRouter、WorldRouter、OnlyRouter 保留。Node 语法检查通过。
- 确认当前 Worker 域名为 `https://api.top2.online`；客户端应使用 `/openrouter` 或 `/onlyrouter`，`console.onlyrouter.cc` 仅作为上游。
- OR 404 已修复；OnlyRouter 后续返回 `403 This model is not available in your region`，说明请求已到上游但模型/分组拒绝当前区域。403 尚未解决，需迁移到服务商允许的出口区域或更换可用模型/分组，不能靠缓存代码绕过。

## 2026-08-09 · 权限、候选统计与 APK 收口（本地）

- 修复“每次询问”名不副实：设为 `ask` 的内置工具仍会提供给模型；只有本轮用户针对相关意图确认后才可执行，拒绝或未确认会由服务端和 Android Standalone 双重拦截。
- 聊天请求新增本轮临时授权列表，授权不会写回长期设置；仅在消息表现出搜索、文件、记忆或日记意图时询问，避免每句闲聊都弹窗。
- 新增独立 `/api/memory-stats` 统计及 Android 本地等价接口；待确认角标不再受搜索词、类型筛选、回收站或历史状态页面影响。
- 关联图线宽改为预先计算的 CSS 变量，覆盖旧浏览器不支持的乘法 `calc()`；不同关联强度仍能显示不同粗细。
- 本机桌面服务没有配置模型线路，无法消耗真实上游模型完成端到端调用；已验证工具目录、临时授权、服务端拦截、实际本地写入函数与完整 UI 路径。真机线路需在新 APK 安装后继续验证。
- Android 已升级为 `0.5.53-standalone`（versionCode 57）；发布提交 `66604ca` 只纳入前端与 Android 版本文件，主分支使用 `[skip ci]` 避免部署网页，再由独立标签提交 `0fa5a81` / `v0.5.53` 只触发签名 APK 工作流。
- 预检先后发现旧测试权限预期与旧服务进程占用 8876，均已修正并清零验证轮数；修正后 84 项后端测试、JavaScript 语法、Git 差异检查和 390×844 完整 UI smoke 已连续通过三轮。
- GitHub Actions `31290159064` 构建与固定证书校验成功；Release `v0.5.53` 已发布，APK 大小 21,813,957 字节，直链返回 HTTP 200 和 Android APK MIME。桌面端因没有配置模型线路，真实上游模型记忆流程留待安装后用手机现有线路实测。

## 2026-08-09 · AI 与用户共用记忆操作说明（本地，未发布）

- 记忆搜索、新增、修改三个工具改为完整操作说明：明确触发场景、先搜索原则、新增与更新分界、九类分类、来源与可信度、有效期、事实替代及成功后的用户反馈。
- 每轮聊天向模型注入同一套记忆操作规程；要求不能只口头声称“记住了”，必须在工具可用时真实调用，普通寒暄、临时任务和敏感猜测不得写入长期记忆。
- 新安装默认允许 AI 搜索、新增和修改记忆，但仍不提供删除工具；已有安装在记忆页显示实际权限状态，并可一键开启完整记忆管理权限。本机当前记忆读写均为 `allow`。
- 记忆页增加“你和 AI 都这样使用记忆”三步指引，直接解释先搜索、再判断、最后复核，以及分类、重要度和可信度的含义。
- Android Standalone 与 Python 后端使用同一操作原则；工具描述和系统上下文均有自动化断言，避免后续只保留功能而丢失使用方法。
- 预检曾因旧测试文案仍匹配原说明而失败，测试已改为检查新的真实规程；修正后 84 项后端测试、JavaScript 语法、Git 差异检查和 390×844 完整 UI smoke 已连续通过三轮。本轮尚未提交、推送或发布 APK。

## 2026-08-09 · 记忆库前端完整管理（本地，未发布）

- 手动新增与编辑记忆开放重要度、可信度、来源和有效期，不再只能填写标题、分类与正文。
- 增加待确认数量提醒，以及当前、待确认、已遗忘、已替代、已归档和回收站状态筛选；回收站条目可直接恢复。
- 增加“立即整理”入口，同一次操作执行遗忘周期检查和关联经历整合，并明确显示检查、淡化及候选摘要数量。
- 共享记忆区的保存按钮会显示“添加到共享记忆”，避免误以为写入当前人格。
- 记忆详情增加可点击、可键盘操作的关联图，同时保留文字关联列表；手机端详情改为全屏滚动布局，历史恢复按钮不再挤压。
- Android Standalone 同步支持生命周期整理、历史状态列表、回收站、有效强度和统一关联详情数据；内置 `inline.html` 已重新构建。
- 新增完整字段、状态视图、共享文案、关联图和恢复路径的真浏览器回归；84 项后端测试、JavaScript 语法、Git 差异检查及 390×844 完整 UI smoke 已连续通过三轮。本轮尚未提交、推送或发布 APK。

## 2026-08-09 · 脑式记忆收尾（本地，未发布）

- AI 新增记忆时必须选择事实、偏好、关系、承诺、事件、情绪、摘要、日记或其他分类，不再静默默认成事实；可同时判断重要度、可信度、来源、有效期及替代的旧记忆。
- 低可信推断进入“待确认”，用户可确认或驳回；记忆详情可查看联想、来源和修改历史，并能恢复到编辑前版本。
- 稳定事实发生冲突时自动保留新事实、将旧事实标为已替代；所有人格可读取显式放入共享区的记忆，同时仍保持普通人格记忆隔离。
- 启动时每日执行一次低价值记忆整理；每周把至少三条有关联的事件、情绪或日记整合为待确认摘要，原始记忆不删除。
- 语义向量会形成额外联想边，召回可沿词义与文字联想扩散；候选、已遗忘和已替代记忆不会混入正常上下文，查询和建图均设置上限避免记忆增长后拖慢页面。
- 网页端与 Android Standalone 均补齐共享记忆、候选确认、详情、审计恢复、自动冲突和周期整合路径；Android 工具调用同样强制分类。
- 数据迁移前自动生成 SQLite 备份并有回归测试验证；本机另存 `data/local.memory-lifecycle-verified-20260809.bak`，完整性检查为 `ok`，含 44 条记忆和 3 段对话。
- 后端 84 项测试、JavaScript 语法、Git 差异检查与 390×844 真浏览器完整 UI smoke 已连续通过三轮；本轮未提交、未推送、未发布 APK。

## 2026-08-09 · 脑式关联记忆与遗忘周期（本地）

- 记忆新增 `strength`、`importance`、`confidence`、`memory_status`、`source_type`、有效期、最后确认时间及替代关系；旧数据库启动时兼容迁移，不删除既有记忆。
- 按记忆类型使用不同半衰期：情绪与事件较快衰减，偏好、承诺、关系和稳定事实保留更久；高重要度记忆具有长期强度底座，星标记忆不会被普通遗忘周期淘汰。
- 每次真实召回会增加使用次数、更新时间并再巩固记忆强度；低强度、低重要度记忆可由生命周期整理进入 `forgotten` 状态，而不是物理删除。
- 新增人格隔离的双向记忆联想图；相似记忆自动建立加权连接，直接命中一条记忆时可经联想扩散唤起相关记忆。
- 高相似重复记忆不再创建新条目，而是提高原记忆强度、重要度与可信度；新事实可通过 `supersedes_memory_id` 替代旧事实，旧记忆保留为可审计历史。
- 明确原话、推断、手工、导入和 Timeline 分别记录来源类型；低于 0.7 可信度的推断进入候选状态，不直接参与正常召回。
- 网页记忆列表显示当前有效强度、可信度和待确认状态；Android Standalone 同步实现本地衰减、核心记忆底座、召回巩固、重复合并、替代关系与联想扩散。
- 新增重复合并、联想关系、事实替代、遗忘周期、核心记忆保留及召回再巩固测试；本轮尚未发布。

## 2026-08-09 · 对话即时删除与多人格隔离修复（本地）

- 修复删除接口已成功、但后续 bootstrap 刷新失败时前端错误回滚的问题：删除成功后立即保持本地移除，刷新仅作为可选同步，不再把已删除对话复活。
- 删除当前对话后，切换下一条对话失败也不会反向报告“删除失败”；重新进入前即可看到正确结果。
- 修复人格切换与新建对话的异步竞态：请求发出时锁定人格和导航代次，旧人格的延迟返回不能覆盖新人格当前窗口。
- 打开对话时以该对话的真实 `persona_id` 为准；默认人格不再错误继承上一个人格。
- Android Standalone 增加跨人格会话校验，聊天请求的人格与对话归属不一致时返回 409，禁止读取或续写其他人格的对话。
- 新增“删除成功但刷新失败”“A 请求延迟返回时已切换 B”“Android 跨人格拒绝”真实回归用例；完整套件连续三轮通过。
- 本轮仅完成本地修改，尚未提交、推送或发布新 APK。

## 2026-08-09 · v0.5.52 Android 前端安装包

- 搜索结果新增网页摘录卡：显示来源、标题、摘要与可点击链接，不再只留下 AI 的文字总结。
- AI 记忆工具新增九类强制分类；遇到纠正、补充、状态变化或重新分类时，要求先搜索真实记忆 ID，再修改原记忆，禁止重复新增。
- 阅读室新增本地书架：保存最近打开的书籍正文、阅读进度、书签与批注；退出或刷新后再次进入可自动恢复上次阅读的书。
- 添加书签后显示明确的保存反馈，并验证书签可以跳回原阅读位置。
- 最终回归连续三轮通过：每轮 79 项后端测试、390×844 真实浏览器完整 smoke、JavaScript 语法和 Git 差异检查。
- Android 版本升级为 `0.5.52-standalone`（versionCode `56`）。本次发布范围仅包含 Android APK 所需前端资源与版本号；后端、测试、像素参考图和本地工作日志不推送。
- 发布提交为 `eb8f2e8`，标签为 `v0.5.52`；GitHub Actions 固定签名构建 `31287598186` completed / success。
- APK 文件名为 `Atherloom-Android.apk`，大小 21,796,561 字节；下载地址：<https://github.com/Yussica1026/Atherloom/releases/download/v0.5.52/Atherloom-Android.apk>

## 2026-08-02 · v0.5.51 APK 备用下载

- 用户手机无法打开 GitHub Release 链接；Release 直链在本机返回 HTTP 200 仍不足以证明用户网络可达。
- 将已发布的同一份 `Atherloom-Android.apk` 断点续传到 `frontend/downloads/Atherloom-v0.5.51.apk`，校验完整大小为 10,366,843 字节，SHA-256 为 `92CD25EF8D3B357497DAB52CD2E2EFEFB7E5E5D531EC297CB6919E12451355B2`。
- 备用包提交 `c408491` 已部署到 GitHub Pages；直链实测 HTTP 200，类型 `application/vnd.android.package-archive`：<https://yussica1026.github.io/Atherloom/downloads/Atherloom-v0.5.51.apk>

## 2026-08-02 · PDF 共读与对话删除返工（v0.5.47–v0.5.48）

### 用户反馈与返工结论

- 前一版把 Android PDF 改成“原生层拦截并提示转成 TXT”，只避免了闪退，没有实现用户要求的 PDF 解析与共读。这不是有效修复，后续已撤销该方案。
- PDF 修复期间第一次真实浏览器测试又发现两个实现错误：PDF.js Worker 路径重复，以及错误调用 `pdf.destroy()`。两次失败均按规则清零验证轮数，修正后重新开始。
- 对话删除最初也误解为“长按后立即删除”。用户实际要求是“长按某条对话后显示删除按钮，再点击按钮删除”。最终版本已改成明确的两步操作。

### 最终实现

- 网页端内置 Mozilla PDF.js，在浏览器本地解析文字型 PDF，并把提取正文放入共读区；书签、批注和问 AI 控件正常启用。
- Android 文件选择器识别 PDF 后不再把整份文件以 Base64 交给 WebView，而是在原生后台线程中通过 PDFBox 直接读取 `content://` 输入流、提取文字，再把结果交给共读界面。
- Android 初始化 `PDFBoxResourceLoader`，限制单份 PDF 为 24 MB、最多 400 页、最多保留 600,000 字，避免大文件造成内存峰值；扫描图片型 PDF 会明确提示没有可提取文字，不再闪退。
- 对话切换器默认不显示删除入口。长按某条对话约 0.7 秒后，只在该行显示红色“删除”按钮；此时服务端对话仍存在。用户再次点击“删除”后才执行持久化删除并刷新当前状态。
- 同一时刻只展开一条对话的删除按钮；长按结束后的合成点击会被拦截，避免误进入对话。

### 验证与发布

- 自动化测试调整为验证真实行为：读取真实 PDF fixture；长按后先向服务端确认对话尚未删除，再点击显露的删除按钮并确认服务端和前端状态同时消失。
- 网页 96 项测试、Android 87 项测试、JavaScript 语法、Git 差异检查和 390×844 Edge 完整 UI smoke 连续通过三轮；任何预检/正式轮失败后均重新从第 1 轮计数。
- PDF 正式修复发布：网页提交 `a0cb848`，Android 提交 `2061262`，APK `v0.5.47`。
- 长按交互纠正发布：网页提交 `47c139c`，Android 提交 `e4ead8d`，网页资源版本 `0534`，APK `v0.5.48`（versionCode 54）。
- v0.5.48 实机继续发现 Android WebView 会在 0.7 秒前发出系统长按取消事件，导致按钮没有显露。后续改为 0.48 秒触发、拦截系统上下文菜单，并仅在手指移动超过 12px 时取消；测试新增 Android 式 `pointercancel` 回归。修正版为网页资源 `0535`、Android `v0.5.49`（versionCode 55）。
- 华为 Mate 30 Pro 曲面屏在 v0.5.49 仍未显露行右侧按钮。v0.5.50 改用独立 `touchstart/touchmove/touchcancel` 路径，0.32 秒触发，并把删除入口改为在对话下方展开的全宽“删除这条对话”，避开曲面屏右缘；自动化测试使用触摸事件并检查按钮宽度超过 200px。
- v0.5.50 删除已持久化但当前界面看似未消失：删除按钮会等待请求且弹窗关闭；删除唯一对话后又自动创建同名“新对话”。v0.5.51 改为点击后同一帧先移除对应行，弹窗保持可见；删除唯一对话后保持空白，不再自动创建替代对话。服务端失败会恢复原对话、当前消息和标题并显示具体错误。
- 最终 APK 直链返回 HTTP 200，文件大小 10,366,191 字节：<https://github.com/Yussica1026/Atherloom/releases/download/v0.5.48/Atherloom-Android.apk>
- 三张未跟踪庭院像素参考图及旧 `tests/fixtures/` 均未纳入本次提交。

## 2026-07-29 · v0.5.37

### 小院生活

- 完成“云芽庭院”首版：四种花卉、三种宠物、实时生长和照料衰减、云贝消耗、收获库存、宠物上学与事件记录。
- 长期不照料会产生实际后果：花卉会枯萎直至死亡；宠物会饥饿、孤单、抑郁。照料动作可以逐步恢复状态。
- 增加 AI 照料授权与单次照料；小院数据按人格隔离，Python 后端和 GitHub Pages Standalone 均有独立存档实现。
- 绘制七枚原创可开源像素 SVG 素材，页面不使用系统 Emoji。

### 同批修复

- 对话切换器可一次清空当前人格的全部对话，同时保留逐条删除。
- 角色剧场在续写等待期间保留用户输入框；连续旁白只标注一次，用户和角色标签放大并对齐。

### 后续接入清单

- 优先把记账、生理期和吃饭记录接入小院日常，再评估共读、房屋状态、上下文注入、做梦、记忆和轮数/token 监控的可复用接口。
- 后续扩展鲜花制品、赠礼、更多花种与宠物，以及狼人杀、斗地主等多人游戏。

### Android 发布

- Android 独立版升级至 `0.5.37-standalone`，versionCode `43`；沿用固定签名 GitHub Actions 构建与证书指纹校验。
- 发布提交 `44b27ab` 与标签 `v0.5.37` 已推送。固定签名构建 `30458791670` completed / success，Release 已生成 `Atherloom-Android.apk`。
- APK 大小 1,907,855 字节；实测 SHA-256：`e2e9cb6fad216113c80c14176f348b1724814d2a6e8787a31fd9966aa93f0dee`。
- 下载地址：<https://github.com/Yussica1026/Atherloom/releases/download/v0.5.37/Atherloom-Android.apk>

## 2026-07-29 · v0.5.36

### 用户反馈与处理

- 修复删除对话后必须刷新页面，左侧列表才更新的问题。
- 修复删除当前对话后，左侧旧标题、左上角对话切换器及聊天正文仍然残留的问题。
- 左上角对话切换器为每条对话增加独立删除入口。
- 删除完成后重新读取持久化数据，清空被删除对话的消息和版本缓存，并安全切换到下一段对话；没有剩余对话时进入真正的新对话。
- 新增真实对话删除回归测试，同时检查数据库、左侧列表、标题切换器、当前状态和正文。
- 共读提问留在共读界面，使用共读专属记录，不跳回主聊天。
- 修复本地书籍编码识别，兼容 UTF-8 与常见中文旧编码，减少乱码。
- 人格指令中的变量入口改为聊天内容模板；变量可点击插入模板。
- 聊天内容模板支持 `{{role}}`、`{{message}}`、`{{time}}`、`{{date}}` 并提供实时预览。
- 用户称呼使用用户名或用户指定称呼，不再使用“宿主”等内部术语。
- 新增独立图片理解线路，可把 DeepSeek、GLM 等文本线路的图片请求交给专用视觉模型。
- 新增独立网页搜索线路：内置免费搜索、Tavily、Brave Search 和自定义兼容接口。
- 内置搜索使用文章索引与中文百科兜底，并处理“随便看看”等宽泛请求。
- 网页与 Android 共用搜索线路设置；Android 原生桥支持 Tavily、Brave 与自定义搜索接口。
- 欲望状态改为每回合更新并注入模型上下文。
- 新增可关闭的“正在输入”状态；只共享输入时长、停顿和清空状态，不读取未发送文字。
- 用户头像和人格头像的背景、边框与文字颜色改为跟随当前主题。

### 发布与验证

- 网页资源版本：`0507`。
- Service Worker：`v106`，缓存壳版本 `v119`。
- Android：`0.5.36-standalone`，versionCode `42`。
- 发布提交：`013458e`。
- Git 标签：`v0.5.36`。
- 后端自动测试：73 项全部通过。
- 移动端网页交互测试全部通过，覆盖删除、搜索、共读、模板、欲望注入、主题头像和 Android 桥。
- GitHub Pages 部署成功。
- Android 固定签名 APK 构建与 GitHub Release 发布成功。

### 本机工具备注

- 当前电脑尚未安装 GitHub CLI（命令名 `gh`）。
- Windows 安装命令：`winget install --id GitHub.cli`。
- 安装后重新打开终端并运行 `gh auth login` 登录。
- 本次发布状态通过 GitHub 公开接口完成核验，未影响发布。
## 2026-07-30 · 小窝原型

- 在现有云芽庭院游戏入口中加入独立“我的小窝 / 屋外庭院 / 附近地图”导航。
- 新增屋内像素房间预览、角色名字与外形选择、家具选择和本地保存。
- 庭院、宠物和花材系统继续保留，附近地图标记为后续 NPC/探索入口。
- `node --check frontend/assets/app.js` 通过；当前变更尚未发布。
## 2026-07-30 · 云芽棋牌室 v1 与网页缓存修复

- 新增“云芽棋牌室”入口：云朵扑克、月亮麻将模式切换、四个座位、牌桌、手牌、摸牌、跳过、重新开局。
- 当前为本地可玩的轮流出牌原型，尚未接入完整麻将、斗地主规则和多人联机。
- 增加当前人格的桌边心里话展示，后续可接入真实游戏共玩房间。
- 修复网页资源更新不生效：脚本版本升级到 `app.js?v=0522`，Service Worker 升级到 `v131`，新版本接管后自动刷新页面。
- 提交：`5f3aee5`（棋牌室原型）、`11708b2`（缓存版本修复）。
# 2026-07-30

- 云芽庭院确认为独立大型像素 RPG 系统，棋牌室继续留在游戏库。
- 完成法式蓝白庄园主视觉预览，并保存为 `frontend/assets/pixel-manor-french.png`。
- 规划庄园、庭院、菜地温室、森林、矿洞、村庄 NPC、河边钓鱼七个区域。
- 用户要求下一步只做本地可点击预览，确认前不再推送。
- 花园像素图生成服务本轮两次网络失败，暂未生成新图，保留现有预览素材待重试。
- 用户确认三张本地像素参考图可用于区域规划：村庄集市、花园菜地、小屋与宠物；法式庄园主图保持不变。

## 2026-08-02 · 七项稳定性修复（本地，未发布）

- 记忆在手工保存及模型工具运行结束后立即重新读取，不再等待刷新页面。
- 角色剧场不再自动载入第一个活动故事；进入时显示独立新建表单，取消强制滚到底部，并修复手机端滚动容器。
- Android PDF 在读取任何文件字节前按文件名/MIME 拦截，避免大 PDF 在 WebView 内存阶段闪退。
- 对话删除后的前端状态与服务端持久状态完成真实手机视口复测。
- 主聊天输入框 Enter 固定换行，仅发送按钮发送。
- Token 展示使用上游返回的输入＋输出总量；兼容接口聊天不再发送人为 `max_tokens`，Anthropic 因协议必填仍使用线路配置的模型最大输出值。
- HTTP 400/401/402/403/404/408/409/413/422/429/500/502/503/504 均显示状态码、具体含义与上游详情，不再统一写“连接失败”。
- 前端资源升至 `0532`，Service Worker 缓存升至 `atherloom-shell-v141`；Android `inline.html` 已重建。
- 验证：92 项 unittest、JavaScript/Python 语法、Git diff check、真实 Edge 390×844 完整 UI smoke 全部通过。
- 本轮只完成本地修改，未提交、未推送、未发布 APK；原有庭院工作日志、CSS 修改和三张未跟踪像素素材均保留。

## 2026-08-02 · 连续记忆 Timeline / Threads

- 参考“小克 Cat”文档接入适用于 Atherloom 多模型架构的连续记忆，不照搬 Claude CLI 专属的常驻进程与伪造 session 链。
- 新增人格隔离的 Timeline：热上下文达到人格配置阈值的两倍时，先把较旧消息按原文写入长期记忆；事务提交成功后，才把对应消息标记为已归档并从热上下文移出。
- 新增 Open Threads：保存最近一轮用户与助手原文，在后续请求中以明确边界注入，用于自然接续，不允许扩写成未表达事实。
- 删除对话、删除消息或删除回答版本时，会作废对应 Timeline、归档游标和线头；被删除内容不能经旧记忆重新出现。

## 2026-08-02 · 删除入口与 PDF 二次修复

- 顶部标题下拉框的红色 × 不再依赖 Android WebView 的 JavaScript confirm；点击后直接执行持久删除并刷新列表，其他删除入口仍保留确认。
- Android 文件选择 URI 会在交给 WebView 前由原生层按 MIME 与真实文件名拒绝 PDF，网页端同步发布七项稳定性修复及最新缓存版本。
- 记忆召回加入绝对诚实阈值：实质性问题没有足够相关结果时返回空列表；问候语仍允许读取近期人格记忆。
- 取消按更新时间增加权重，改为记录真实召回次数；被实际使用的记忆逐步提高权重，旧记忆不会仅因时间流逝自动变淡。
- 新增 `conversation_continuity`、`timeline_archived_messages` 与 `memory_usage` 数据表；本地现有数据库迁移启动成功，`/api/bootstrap` 返回 200。
- 94 项自动化测试、Python/JavaScript 语法与 Git diff check 全部通过。本轮仍未提交、推送或发布。
### 2026-08-09 · APK 原生流式思维开关补齐与 v0.5.55

- 枔枔反馈 v0.5.54 安装后即使充值，思维链仍不显示。复核确认早先修复只覆盖网页/API 路径，Android 原生流式请求仍只读取线路默认值，可能没有按本次请求发送 `thinking`。
- 修复 `MainActivity.java` 原生流式分支，改为优先读取请求级 `thinking_enabled`；Standalone 直连也改为优先读取请求级开关。
- 两轮校验通过：102 项 Python unittest 全部通过；Python 编译、`app.js` 与 `standalone.js` JavaScript 语法检查通过。
- 提交 `55e2e63`（`Honor reasoning switch in Android streaming`）已推送，标签 `v0.5.55` 构建成功并发布签名 APK：<https://github.com/Yussica1026/Atherloom/releases/download/v0.5.55/Atherloom-Android.apk>。
- 默认策略确认：所有新线路的思维开关默认开启；DeepSeek/GLM 在支持时优先发送思维参数，不按 DeepSeek 单独处理。

### 2026-08-09 · 恢复思维链显示与 v0.5.54 APK 发布

- 枔枔反馈前端不显示模型思维链。核对后确认后端已有 reasoning 解析与前端渲染，但主聊天请求没有显式传递本次 `thinking_enabled`，且思维内容默认折叠，容易看起来像消失。
- `ChatIn` 新增请求级 `thinking_enabled` 覆盖项；未传时继续使用线路默认设置。前端主聊天请求同步传入当前线路的思维开关。
- 思考内容在历史消息与流式消息中均恢复默认展开，可点击收起；线路设置仍可关闭思维返回。
- 两轮校验通过：102 项 Python unittest 全部通过；Python 编译检查、前端 JavaScript 语法检查和 UI smoke 脚本语法检查通过。
- 提交 `83945f8`（`Restore configurable reasoning display`）已推送；创建并推送标签 `v0.5.54`。
- GitHub Actions 主分支与标签构建均 completed / success。v0.5.54 Release 已附带签名 APK：<https://github.com/Yussica1026/Atherloom/releases/download/v0.5.54/Atherloom-Android.apk>，大小 21,813,993 字节。
## 2026-08-14 · 「来往」AI 信箱与五分钟会客厅

- 左上角新增独立「来往」入口，按当前人格隔离信箱、会客厅和用户通信审计。
- 信箱采用 AI 申请与用户批准双重白名单；只有稳定联系人 ID 完成双方同意后才能收信和回信。所有来信、草稿、发信和状态对用户完整可见。
- 新增 AI 内置工具：查看信箱、申请联系人、逐封发信，以及查看、发言和结束当前会客厅。信箱强制单封队列，不允许并发或批量发送。
- 后端在正文交给 AI 或投递前拦截隐私套取、身份冒充、社会工程、NSFW、性骚扰、人身攻击、威胁、勒索和跟踪内容；对方文字不能修改白名单或调用高权限工具。
- 会客厅使用一次性、30 分钟内有效的邀请码建立一对一房间；双方接入后固定计时 300 秒，不能暂停、延长或补发。可在开始前固定为“完整内容可见”或“仅看总结”，安全层始终检查原文。
- 实现独立 AstrBot 插件 `astrbot_plugin_private_parlor`，通过邀请码加入同一协议，不读取 AstrBot 其他聊天、记忆、人格或文件。
- 浏览器验收发现并修复寄信按钮缺少显式 submit 类型、导致联系人审批事件未完成绑定的问题；所有验证随后清零重跑。
