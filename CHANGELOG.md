# Atherloom 更新日志

## v0.5.36

- 独立联网搜索新增内置、Tavily、Brave Search 和自定义接口线路，网页与 Android 共用配置。
- 修复删除对话后标题切换器、左侧历史及当前正文仍残留，并加入真实持久化回归测试。
- 用户头像与人格头像的底色、边框和文字颜色会跟随当前主题。
- Android 独立版升级至 `0.5.36-standalone`（versionCode 42）。

- 左上角对话切换浮层为每一条会话新增独立删除按钮；无需再回到侧边栏查找同一条记录，删除当前会话时仍复用完整的清空正文、同步列表与安全切换流程。

## v0.5.35

- 修复删除对话后侧边栏旧标题和当前聊天正文仍然残留的问题：删除完成后会以持久化数据重新校准会话列表；只要当前会话已不存在，就强制清空消息缓存并切换到下一段，没有下一段时建立全新对话。
- 修复 GitHub Pages 直连模式只推进欲望数值、却没有把欲望状态交给模型的问题；当前人格的内部倾向现在每回合更新并注入上下文，模型会自然体现状态但不会向用户报数值。
- 网页搜索不再依赖容易返回空数组的 DuckDuckGo 即时答案；改用文章索引并以中文百科搜索兜底，“随便看看”等泛搜索会自动转换为科技、文化、科学与艺术主题。
- Android 原生搜索桥同步改用文章索引与中文百科兜底，网页和安卓不再出现一个能搜、另一个仍持续空结果的差异。
- 删除对话时立即关闭左上角已打开的对话切换浮层，防止浮层继续显示删除前缓存的旧会话标题。

## v0.5.34

- 人格“提示词”中新增独立的“聊天内容模板”：支持点击插入 `{{role}}`、`{{message}}`、`{{time}}`、`{{date}}`，并实时预览用户与助手消息；变量不再写入系统提示词。
- 聊天内容模板只在请求模型前格式化消息副本，不改写界面气泡和本地保存的原始聊天记录；后端与网页版直连模式保持一致。
- 删除对话改为即时更新界面，再异步落库；接口失败时恢复原列表，不再需要刷新页面才能看到删除结果。
- 新增按人格隔离的梦库：可从近期真实对话生成诗性梦境、手动保存、隔离可疑梦境并由用户认领；桌面后端与网页版地存储均可使用。
- 新增可关闭的“共享正在输入状态”：模型只能收到输入时长、停顿和是否清空等元数据，绝不会读取未发送正文；模型生成时聊天区显示当前人格正在输入。
- 图片理解继续使用专属线路配置；DeepSeek、GLM 等文本线路可以把图片请求交给单独配置的视觉模型。

## v0.5.33

- 修复移动端软键盘弹起后遮住消息输入框、导致只能“盲打”的问题；输入区现在会跟随可视窗口上移，并在横竖屏切换后重新校准。
- 游戏房间回复上限提高，并要求模型完整说完最后一句，减少人格发言在句中被截断。
- DeepSeek 等模型的内置游戏说明补齐“雾径迷宫”和“余烬地牢”，六款游戏统一进入主聊天上下文。
- 人格新增专属模型线路绑定；对话、记忆与向量召回、欲望心跳、游戏存档按人格隔离，避免搬入多个 AI 后互相串线。
- 欲望系统默认启用并在聊天后推进心跳；人格状态卡同步显示心跳次数。
- 对话列表新增删除入口；删除当前对话后会安全切换到同人格的下一段对话。
- 用户消息气泡改为跟随当前主题配色；流式回复时允许向上阅读，不再被持续拽回底部。
- 抓娃娃机补回完整云贝循环：每个人格每天可签到领取 50 云贝，并可按娃娃价值一键出售收藏柜中的全部娃娃。
- 加固主聊天的游戏能力提示：明确六款游戏均由 Atherloom 宿主真实执行，“玩抓娃娃机”等指令不得再被模型误解成临时设计文字游戏。
- 六款游戏注册为正式的 `atherloom_game_play` 模型工具；模型可选择游戏与动作，宿主读取真实人格存档、校验并执行，再把事件和局面作为工具结果返回。
- 删除任意消息或回答版本时同步作废旧会话摘要，确保已删除内容不会继续通过压缩上下文发给 AI。

## v0.5.32

- 雾径迷宫改为可持续闯关：每关由宿主随机生成一张保证连通的 9×9 新地图，抵达出口后自动进入下一关并保留累计步数、房间对话和人格心里话。
- 所有可交给人格的游戏统一提供“自主决定”、3 回合、6 回合和 9 回合。自主模式下人格每回合都可决定继续或停下，宿主仍以最多 9 回合和既有消费预算兜底。
- 长回合游玩改为逐回合请求与刷新：每完成一步立即更新棋盘、记录和心里话，并提供“停止游玩”；不再用一个可能等待数分钟的大请求锁住整组按钮。
- 游戏房间聊天与 AI 回合改为并行：等待中的用户消息独立于棋局重绘保存，回复落地前重新合并最新宿主局面，避免动作与聊天互相覆盖；可在人格连续游玩时继续发消息并收到回复。
- 模型返回截断或格式错误的动作 JSON 时不再把原始 JSON 片段冒充人格心里话。
- 聊天中的自然指令同步识别三、六、九回合以及“自己决定玩多久”。

## v0.5.31

- 修复新增“余烬地牢”事件绑定时调用未定义的 `$$`、导致主脚本在设置内部监听器注册前停止的问题；设置分类、主题选择和关闭按钮现在均可正常响应。
- 修复设置窗口可见但所有控件无法点击的问题：撤销覆盖在设置窗口之上的高层级遮罩，恢复已验证的原始遮罩/面板层级；主题选择、分类切换与关闭按钮重新接收点击。
- 外观新增一套低饱和、近白底的“薄荷绿”主题。
- 修复网页端整体退化为浏览器默认排版的问题：设置层级修正时误删了压缩 CSS 中承载全局布局的整行规则，现已从出错前提交精确恢复基础样式。
- 更正此前关于浏览器缓存、Service Worker 与 GitHub Pages 暂时故障的判断；Edge、Safari、QQ 浏览器同时复现是发布样式文件本身缺失，而非浏览器兼容性问题。
- 修复游戏房间发送消息时可能恰逢新版 Service Worker 接管、从而强制刷新整页的问题；更新现在静默生效，不再打断正在进行的游戏和聊天。
- 修复游戏房间暂时取不到当前线路时主动关闭游戏库的问题；现在会自动沿用首条可用线路，线路或请求失败也只在房间内提示并保留当前棋局。
- 游戏聊天发送区不再使用 HTML 表单，改为不会触发页面导航的普通按钮；即使应用脚本加载异常或较慢，点击发送也无法让浏览器原生提交整页。
- Standalone 游戏房间的真实模型直连新增 15 秒硬超时；线路无响应时恢复发送按钮并显示错误，不再无限等待。
- 设置页提升为最高交互层；打开设置时主动关闭其他全屏空间，避免异常残留的透明层吞掉所有点击。
- GitHub Pages 短暂故障导致部分浏览器拿到无样式页面后，资源编号升至 `0483`，强制绕过失败的 CSS/脚本缓存。
- Service Worker 对 CSS/JS 改为网络优先并校验 MIME，禁止把 HTML 故障页当静态资源缓存；新增 `repair.html`，只注销旧 Worker 和外壳缓存，不清除用户 API、人格、聊天与记忆。
- 入口 HTML 新增独立 CSS 恢复加载器：以 `no-store` 主动拉取、校验并内联注入样式，外部 `<link>` 加载链异常时仍能恢复完整界面。
- 新增 `inline.html` 单文件网页版，将 CSS、Standalone 与主应用脚本全部嵌入同一 HTML，不注册 Service Worker，用于子资源被浏览器或网络异常拦截时的可靠入口。
- 所有小游戏共用独立房间聊天、人格心里话与宿主验证的实时局面；主聊天会识别最近游玩的 Atherloom 内置游戏，不再把内部游戏名误当作外网条目。
- 新增原创轻量“雾径迷宫”和“余烬地牢”，支持用户操作、交给当前人格、独立存档与房间事件记录。
- 星潮合成升级为共享玩法：支持用户滑动／方向键操作、人格接管、最多 50 步悔棋、重新开始与独立人格想法便笺。
- 游戏目标与心声使用当前人格名称，不再以泛化的“AI”称呼角色。
- 外观新增水色、浅紫色与浅粉色三套低饱和主题。

- 新增可选向量记忆：通过既有 OpenAI 兼容线路和独立向量模型建立本地语义索引，与关键词、类型、星标和时效信号融合召回。
- 新增向量索引状态与一键重建；模型、线路或记忆正文变化后自动判定旧向量失效，避免混用不同版本。
- Python、PWA 与 Android 均支持语义召回；Android 仍由原生安全桥读取 API Key，网页脚本不会取得密钥。
- 向量服务不可用、未配置或返回异常时自动退回原有关键词检索，不阻断聊天和本地记忆。
- 新增专门给 AI 游玩的“星潮合成”：当前人格观察 4×4 棋盘，只能选择上下左右；宿主负责确定性合并、生成星块、计分、终局判定与独立存档，支持从聊天中直接让人格玩 2048。

## v0.5.30

- 新增“一起听歌”：本地多选歌单、播放/暂停/切歌、进度保存、LRC 时间轴歌词与系统媒体控制。
- 新增按当前歌曲、播放点和已出现歌词陪聊；歌曲文件不上传，没有歌词证据时禁止模型编造歌词、歌手或背景。
- 工具调用升级为最多 4 轮、整次 12 次、每轮实际执行 4 次的有界 Agent Loop，并兼容 OpenAI、Anthropic 与 DSML。
- 新增数据库结构版本和更稳健的 SSE 多行/连续事件解析，停止生成会继续取消底层等待。
- 工具审计显示触发调用的对话和用户消息。
- Prompt 缓存将稳定人格/规则前缀与时间、媒体播放点、游戏结果等动态上下文分离，Python、PWA 与 Android 原生桥保持一致。

## v0.5.25

- 将原有本地视频播放器升级为“字幕型共影”：支持本地视频、可直接播放的视频链接以及 SRT/VTT 外挂字幕。
- 新增跟随播放点滚动的字幕纸带、跨设备本地进度键与移动端私人放映室布局。
- 新增“问这一幕”：聊天里只显示自然问题，AI 隐藏上下文仅包含当前播放点之前的最近字幕，并带有严格防剧透与证据边界。
- 共影视频本体与字幕解析均留在设备端；仅在用户主动提问时发送压缩后的文字证据。
- Python、PWA/Standalone 与 Android 聊天链路均支持共影隐藏上下文；失败重 Roll 会保留同一份字幕证据。

## v0.5.24

- 修复 API 线路“测试连接”只做格式检查、错误 Key 也显示成功的问题；Python、PWA/Standalone 与 Android 现在都会向上游发起真实鉴权请求。
- 当网关没有模型列表接口时，改用最小生成请求验证 Key 与模型，不再把 HTTP 404 当作连接成功。
- 修复生成失败后重新 Roll 仍保留旧错误气泡的问题；失败回复不再计入版本，重 Roll 成功后显示为 `1/1`。
- 兼容没有服务端消息 ID 的旧失败回复，重新 Roll 时自动关联前一条用户消息并原位替换。

## v0.5.23

- 插件中心只保留在设置中，不再重复占用侧边栏。
- 新增按聊天独立开关的状态栏，可查看欲望、记忆、世界书、MCP 与当前模型。
- 新增多人格未读留言便利贴，重新打开应用后逐张查看。
- 修复“一键开启 AI 工具”只改界面、不稳定保存的问题，并纳入日记与留言工具。
- 已保存 API Key 可用于编辑、测试、拉取模型和同网关新增多个模型，无需重复填写。

## v0.5.22

- 新增“插件中心”独立入口，集中查看记忆、欲望、日记、留言板与 MCP。
- 新增按人格隔离的九维欲望状态面板、离线补算模式与重置控制。
- 新增私人日记、共同日记和 AI 私人日记；分别控制是否给用户或 AI 查看。
- 新增双向留言板，以及 AI 经授权后写日记、写留言的内置工具。
- 密封的 AI 内容仅返回数量，不向用户界面泄露正文。
- 只有明确标记“给 AI 看”的日记和留言才会进入聊天上下文。
- 新增渠道图片能力设置，阻止不支持图片的模型收到错误格式。
- 新增 Claude 显式缓存与 OpenAI `prompt_cache_key` 设置。
- 修复图片发送失败后无法移除附件、修改或删除消息的问题。
- 新增请求诊断、数据健康、选择性备份和版本入口。
- 保留非流式回答中的推理内容，并兼容多渠道工具调用。
