# Atherloom Android v0.5.45

本版本只发布 Android APK，不更新网页端，也不包含云芽庭院。

- 合并持续记忆：时间线归档、开放线头、诚实召回阈值、使用次数权重，以及编辑/删除后的连续性失效。
- 记忆在保存和对话结束后重新读取，界面即时刷新。
- 剧场进入时显示新建页，不再误开“雾港来信”；移除强制滚到底部，并修复手机纵向滚动。
- Android PDF 在读取文件字节前安全拦截，避免共读闪退。
- 保留并验证对话删除的本地持久化行为。
- 聊天输入框 Enter 只换行，发送仅由发送按钮触发。
- token 显示只采用接口返回的完整用量；兼容 OpenAI 的请求不再附加客户端输出上限，Anthropic 保留其必填的模型上限。
- HTTP 400、401、402、403、404、408、409、413、422、429、500、502、503、504 均提供明确含义与上游详情。

验证：JavaScript 语法检查通过，内联 Android 页面已重建，84 项自动化测试通过。
