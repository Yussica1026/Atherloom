# Atherloom Android v0.5.46

- 修复 PDF 仍可能闪退：系统文件选择器返回 URI 后，原生层同时按 MIME 与真实文件名识别 PDF，并在 URI 交给 WebView 前返回空结果。
- 修复顶部标题下拉框红色 × 无法删除对话：该入口不再依赖 Android WebView 不稳定的 JavaScript confirm，点击后直接执行持久删除并刷新列表。
- 其他入口仍保留删除确认，避免误触。
- Android versionCode 52 / versionName 0.5.46-standalone。
- 验证：86 项自动化测试、JavaScript 语法与 diff check 通过。
